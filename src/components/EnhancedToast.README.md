# EnhancedToast Component

Продвинутая система toast-уведомлений с анимациями, прогресс-барами, действиями и стекированием.

## Возможности

- 🎨 **5 типов уведомлений** - success, error, warning, info, loading
- ⏱️ **Progress bar** - визуальный индикатор оставшегося времени
- 🎯 **Действия** - добавление кнопок для быстрых действий
- 🔗 **Ссылки** - прямые ссылки из уведомления
- 📍 **Позиционирование** - 6 вариантов позиции на экране
- ⚡ **Promise handling** - автоматическое обновление состояния для промисов
- 🎭 **Анимации** - плавные входы/выходы с учётом настроек пользователя
- 📚 **Стекирование** - несколько уведомлений одновременно
- ⏸️ **Pause on hover** - пауза таймера при наведении

## Использование

### 1. Добавьте провайдер в layout

```tsx
import { EnhancedToastProvider } from '@/components/EnhancedToast'

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html>
      <body>
        <EnhancedToastProvider position="bottom-right" maxToasts={5}>
          {children}
        </EnhancedToastProvider>
      </body>
    </html>
  )
}
```

### 2. Используйте в компонентах

```tsx
import { enhancedToast } from '@/components/EnhancedToast'

function MyComponent() {
  const handleSave = () => {
    enhancedToast.success('Сохранено!', 'Все изменения успешно применены')
  }

  return <button onClick={handleSave}>Сохранить</button>
}
```

## API

### enhancedToast методы

#### Basic Toasts

```tsx
// Успех
enhancedToast.success('Заголовок', 'Описание')

// Ошибка (длительность 7 секунд)
enhancedToast.error('Ошибка', 'Что-то пошло не так')

// Предупреждение (длительность 6 секунд)
enhancedToast.warning('Внимание', 'Проверьте данные')

// Информация
enhancedToast.info('Информация', 'Новое обновление доступно')

// Загрузка (бесконечная)
const loadingId = enhancedToast.loading('Загрузка...', 'Ожидайте')
```

#### Promise Toast

Автоматически показывает loading → success/error:

```tsx
enhancedToast.promise(
  fetchData(),
  {
    loading: 'Загружаем данные...',
    success: 'Данные загружены!',
    error: 'Не удалось загрузить данные'
  }
)

// С динамическими сообщениями
enhancedToast.promise(
  saveUser(userData),
  {
    loading: 'Сохраняем пользователя...',
    success: (user) => `Пользователь ${user.name} сохранён`,
    error: (err) => `Ошибка: ${err.message}`
  }
)
```

#### Расширенные опции

```tsx
enhancedToast.success('Файл загружен', 'document.pdf успешно загружен', {
  duration: 10000,              // Длительность в мс
  progress: true,               // Показать progress bar
  dismissible: true,            // Можно закрыть вручную
  actions: [                    // Кнопки действий
    {
      label: 'Открыть',
      onClick: () => openFile(),
      variant: 'primary'
    },
    {
      label: 'Удалить',
      onClick: () => deleteFile(),
      variant: 'danger'
    }
  ],
  link: {                       // Ссылка
    href: '/files/123',
    label: 'Перейти к файлу'
  }
})
```

#### Управление

```tsx
// Закрыть конкретное уведомление
const id = enhancedToast.success('Сохранено')
enhancedToast.dismiss(id)

// Закрыть все уведомления
enhancedToast.dismissAll()

// Обновить существующее уведомление
const id = enhancedToast.loading('Загрузка...')
// Позже...
enhancedToast.update(id, {
  type: 'success',
  title: 'Готово!',
  duration: 5000,
  dismissible: true
})
```

## Типы и интерфейсы

### ToastType

```tsx
type ToastType = 'success' | 'error' | 'warning' | 'info' | 'loading' | 'promise'
```

### ToastPosition

```tsx
type ToastPosition =
  | 'top-left'
  | 'top-center'
  | 'top-right'
  | 'bottom-left'
  | 'bottom-center'
  | 'bottom-right'
```

### ToastAction

```tsx
interface ToastAction {
  label: string                               // Текст кнопки
  onClick: () => void                         // Обработчик клика
  variant?: 'primary' | 'secondary' | 'danger' // Стиль кнопки
}
```

### EnhancedToastProps

```tsx
interface EnhancedToastProps {
  id: string                    // Уникальный ID (генерируется автоматически)
  type: ToastType               // Тип уведомления
  title: string                 // Заголовок
  description?: string          // Описание
  duration?: number             // Длительность (мс), 0 = бесконечно
  dismissible?: boolean         // Можно закрыть вручную (по умолчанию true)
  actions?: ToastAction[]       // Кнопки действий
  progress?: boolean            // Показать прогресс-бар
  link?: {                      // Ссылка
    href: string
    label: string
  }
  onDismiss?: () => void        // Колбэк при закрытии
}
```

## Примеры использования

### Сохранение данных

```tsx
const handleSave = async () => {
  const id = enhancedToast.loading('Сохранение...')

  try {
    await saveData(formData)
    enhancedToast.update(id, {
      type: 'success',
      title: 'Сохранено!',
      description: 'Все изменения применены',
      duration: 5000,
      dismissible: true
    })
  } catch (error) {
    enhancedToast.update(id, {
      type: 'error',
      title: 'Ошибка сохранения',
      description: error.message,
      duration: 7000,
      dismissible: true
    })
  }
}
```

### Операция с подтверждением

```tsx
const handleDelete = (letterId: string) => {
  enhancedToast.warning(
    'Удалить письмо?',
    'Это действие нельзя отменить',
    {
      duration: 10000,
      actions: [
        {
          label: 'Удалить',
          onClick: async () => {
            const id = enhancedToast.loading('Удаление...')
            await deleteLetter(letterId)
            enhancedToast.update(id, {
              type: 'success',
              title: 'Письмо удалено',
              duration: 3000,
              dismissible: true
            })
          },
          variant: 'danger'
        },
        {
          label: 'Отмена',
          onClick: () => enhancedToast.dismissAll(),
          variant: 'secondary'
        }
      ]
    }
  )
}
```

### Загрузка файла с прогрессом

```tsx
const handleUpload = async (file: File) => {
  enhancedToast.success(
    'Файл загружается',
    file.name,
    {
      duration: 5000,
      progress: true,
      actions: [
        {
          label: 'Отмена',
          onClick: () => cancelUpload(),
          variant: 'secondary'
        }
      ]
    }
  )
}
```

### Сетевая операция с Promise

```tsx
const syncData = () => {
  enhancedToast.promise(
    fetch('/api/sync', { method: 'POST' }).then(r => r.json()),
    {
      loading: 'Синхронизация данных...',
      success: (data) => `Синхронизировано ${data.count} элементов`,
      error: (err) => `Ошибка синхронизации: ${err.message}`
    }
  )
}
```

### Уведомление с ссылкой

```tsx
const notifyNewComment = (letterId: string, author: string) => {
  enhancedToast.info(
    'Новый комментарий',
    `${author} оставил комментарий`,
    {
      duration: 8000,
      link: {
        href: `/letters/${letterId}`,
        label: 'Открыть письмо'
      }
    }
  )
}
```

### Batch операции

```tsx
const handleBulkDelete = async (ids: string[]) => {
  const promises = ids.map(id => deleteItem(id))

  enhancedToast.promise(
    Promise.all(promises),
    {
      loading: `Удаление ${ids.length} элементов...`,
      success: `Удалено ${ids.length} элементов`,
      error: 'Не все элементы удалены. Попробуйте снова.'
    }
  )
}
```

## Интеграция с существующим кодом

Вместо старого `toast` из `sonner`:

```tsx
// Старый код
import { toast } from '@/lib/toast'
toast.success('Сохранено')

// Новый код
import { enhancedToast } from '@/components/EnhancedToast'
enhancedToast.success('Сохранено')
```

Обе системы могут работать одновременно. `EnhancedToast` предоставляет дополнительные возможности.

## Стилизация

Цвета и стили автоматически адаптируются под тему приложения:

- **Success**: Emerald (зелёный)
- **Error**: Red (красный)
- **Warning**: Amber (янтарный)
- **Info**: Sky (голубой)
- **Loading**: Gray (серый)

## Accessibility

- ✅ ARIA labels для кнопок
- ✅ Keyboard navigation (Tab для перемещения, Enter для действий, Escape для закрытия)
- ✅ Focus trap внутри уведомления с действиями
- ✅ Screen reader friendly

## Производительность

- **Lazy rendering** - рендерятся только видимые toast'ы
- **Auto cleanup** - автоматическое удаление после анимации выхода
- **Throttled updates** - оптимизированные обновления при множественных вызовах
- **GPU acceleration** - использование CSS transforms для анимаций

## Настройки

### EnhancedToastProvider props

| Prop | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `position` | `ToastPosition` | `'bottom-right'` | Позиция на экране |
| `maxToasts` | `number` | `5` | Максимум одновременных toast'ов |
| `children` | `ReactNode` | **required** | Дочерние компоненты |

## Интеграция с пользовательскими настройками

Компонент автоматически учитывает `preferences.animations`:

```tsx
const { preferences } = useUserPreferences()
// Если preferences.animations === false, анимации отключены
```

## Отличия от Sonner

| Возможность | EnhancedToast | Sonner |
|-------------|---------------|--------|
| Progress bar | ✅ | ❌ |
| Действия с вариантами | ✅ | Ограничено |
| Pause on hover | ✅ | ✅ |
| Promise handling | ✅ | ✅ |
| Ссылки | ✅ | ❌ |
| Стекирование | ✅ (с лимитом) | ✅ (unlimited) |
| Обновление toast | ✅ | ✅ |
| Позиционирование | 6 позиций | 9 позиций |
| Размер бандла | Больше | Меньше |

## Best Practices

1. **Используйте подходящие типы**:
   - `success` - для успешных операций
   - `error` - для ошибок (автоматически 7 сек)
   - `warning` - для предупреждений, требующих внимания
   - `info` - для информационных сообщений
   - `loading` - для длительных операций

2. **Duration**:
   - Короткие сообщения: 3-4 сек
   - Средние сообщения: 5-6 сек
   - Сообщения с действиями: 8-10 сек
   - Ошибки: 7+ сек
   - Loading: 0 (бесконечно)

3. **Действия**:
   - Не более 2-3 действий
   - Используйте `variant` для визуальной иерархии
   - Деструктивные действия - `variant: 'danger'`

4. **Progress bar**:
   - Используйте для операций с известной длительностью
   - Не используйте для loading toast'ов

5. **Ссылки**:
   - Для навигации к связанному контенту
   - Используйте короткие лейблы ("Открыть", "Перейти")
